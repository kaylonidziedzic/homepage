# 极简导航页 (Minimal Nav)

一个纯静态、高颜值的个人导航页。采用 iOS/macOS 风格的“白玻璃”设计，支持分组、搜索、移动端适配以及数据导入导出。

![预览图](https://images.unsplash.com/photo-1493246507139-91e8fad9978e?q=80&w=1200&auto=format&fit=crop)
## ✨ 特性

- 🍎 **iOS 风格设计**：清爽的磨砂玻璃质感，适配浅色壁纸。
- 📂 **自动分组**：根据“标签”自动将服务归类显示。
- 📱 **完美适配移动端**：手机上自动切换为双列布局 + 底部抽屉式弹窗。
- ⚡ **纯静态零依赖**：没有后端数据库，直接丢到任何静态托管 (Vercel/Cloudflare/Nginx) 即可运行。
- 🔒 **安全隐私**：数据仅保存在浏览器 LocalStorage，支持导出 JSON 备份。
- 🎨 **智能图标**：支持 Emoji、图片链接，或自动生成的马卡龙色首字母图标。

## 🚀 一键部署

### 1. Vercel 部署 (推荐)
点击下方按钮，直接将本仓库克隆到你的 Vercel 并自动构建：

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/你的用户名/你的仓库名)

> **注意**：请先将上面的链接替换为你自己的 GitHub 仓库地址。

### 2. Cloudflare Pages 部署
Cloudflare 操作非常简单：

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)。
2. 进入 **Workers & Pages** -> **Create Application** -> **Pages** -> **Connect to Git**。
3. 选择你 Fork/上传好的这个 GitHub 仓库。
4. **Build settings** (构建设置) 保持默认：
   - Framework preset: `None` (空)
   - Build command: `(空)`
   - Build output directory: `(空)` 或 `/`
5. 点击 **Save and Deploy** 即可。

### 3. Docker 部署
如果你有自己的服务器或 NAS：

```bash
# 构建镜像
docker build -t my-nav .

# 运行容器 (映射 8080 端口)
docker run -d -p 8080:80 --name my-nav my-nav
