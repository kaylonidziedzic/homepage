version: '3.8'

services:
  homepage:
    build: .
    container_name: personal-homepage
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
